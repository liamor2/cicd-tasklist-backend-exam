{
  "name": "tasklist-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "prisma generate && tsc",
    "start": "node dist/server.js",
    "test": "vitest run --config vitest.config.ts unit",
    "test:coverage": "vitest run --config vitest.config.ts unit --coverage",
    "test:e2e": "vitest run --config vitest.config.ts e2e",
    "test:e2e:coverage": "vitest run --config vitest.config.ts e2e --coverage",
    "prisma:migrate": "prisma migrate dev",
    "prisma:generate": "prisma generate",
    "docker:build": "docker build -t efrei-pro-pipepline-exam-backend:latest .",
    "docker:up": "./scripts/docker-compose.sh up --build",
    "docker:down": "./scripts/docker-compose.sh down",
    "trivy": "npm run trivy:scan",
    "trivy:build": "docker build -t efrei-pro-pipepline-exam-backend:latest .",
    "trivy:scan": "mkdir -p reports && docker run --rm -v /var/run/docker.sock:/var/run/docker.sock -v \"$(pwd)/reports:/reports\" -v cicd-tasklist-backend_trivy-cache:/root/.cache/trivy aquasec/trivy:latest image --scanners vuln --exit-code 1 --severity CRITICAL,HIGH --ignore-unfixed --no-progress --format json --output /reports/trivy-vulnerabilities.json efrei-pro-pipepline-exam-backend:latest",
    "trivy:sbom": "mkdir -p reports && docker run --rm -v /var/run/docker.sock:/var/run/docker.sock -v \"$(pwd)/reports:/reports\" -v cicd-tasklist-backend_trivy-cache:/root/.cache/trivy aquasec/trivy:latest image --format cyclonedx --output /reports/sbom.cdx.json efrei-pro-pipepline-exam-backend:latest",
    "trivy:ci": "npm run trivy:build && npm run trivy:scan && npm run trivy:sbom",
    "sonar": "set -a && . ./.env && set +a && npm run sonar:ci",
    "sonar:all": "npm run sonar && sh -c 'cd ../cicd-tasklist-frontend && npm run sonar'",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "lint": "biome lint .",
    "check": "biome ci .",
    "sonar:ci": "docker run --rm --user 0:0 -e SONAR_USER_HOME=/tmp/.sonar -v \"$(pwd):/usr/src\" -v \"$(basename $(pwd))_sonar-cache:/tmp/.sonar/cache\" sonarsource/sonar-scanner-cli:latest -Dsonar.host.url=\"$SONAR_HOST_URL\" -Dsonar.token=\"$SONAR_TOKEN\" -Dsonar.projectKey=\"$SONAR_PROJECT_KEY\" -Dsonar.qualitygate.wait=true -Dsonar.qualitygate.timeout=300"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "prisma": "^6.9.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.1",
    "@sonar/scan": "^4.3.8",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.2",
    "@types/node": "^22.15.0",
    "@types/supertest": "^6.0.2",
    "@vitest/coverage-v8": "^3.2.6",
    "supertest": "^7.1.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.1"
  }
}
